#include <stdio.h>
#include <stdlib.h>
int main(int argc, char *argv[])
{
  int total;

  if (argc < 2)
    {
      printf("error\n");
      return (1);
    }
  total = atoi(argv[1]) * atoi(argv[2]);
  printf("%d\n", total);
  return (0);
}
